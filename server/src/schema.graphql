type Query {
  wallet(stakeAddress: String!): Wallet!
  hello: String!
}

type Wallet {
  balance: String!
  marketPrice: Float!
  txs: [Tx!]!
}

type Tx {
  type: TxDirection!
  amount: String!
  fees: String
  date: Int!
  id: String!
}

enum TxDirection {
  Outgoing
  Incoming
}
