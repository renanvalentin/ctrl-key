schema: src/schema.graphql
# documents: src/**/*.{graphql,ts}
extensions:
  codegen:
    generates:
      ./src/resolvers-types.ts:
        plugins:
          - typescript
          - typescript-resolvers
